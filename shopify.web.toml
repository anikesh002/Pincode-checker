name = "remix"
roles = ["frontend", "backend"]
webhooks_path = "/api/webhooks"
[commands]
predev = "npm exec prisma generate"
dev = "npm exec prisma db push --accept-data-loss && npm exec remix vite:dev"
